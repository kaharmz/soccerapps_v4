<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.soccerapps">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.activity.SearchTeamActivity"></activity>
        <activity android:name=".view.activity.DetailPlayerActivity" />
        <activity android:name=".view.activity.DetailTeamActivity" />
        <activity android:name=".view.activity.TeamEventActivity" />
        <activity
            android:name=".view.activity.ListEventActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".view.activity.DetailLeagueActivity"
            android:label="Detail league" />
        <activity android:name=".view.activity.SearchEventActivity" />
        <activity android:name=".view.activity.DetailEventActivity" />
        <activity
            android:name=".view.activity.MainActivity"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>